{
  "name": "no.runely.calendar",
  "version": "0.0.1",
  "main": "app.js",
  "repository": {
    "url": "https://github.com/runely/calendar-homey"
  },
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "homey": "2.23.3",
    "jest": "27.3.1",
    "standard": "16.0.4"
  },
  "dependencies": {
    "@sentry/node": "6.13.3",
    "humanize-duration": "3.27.0",
    "moment": "2.29.1",
    "node-ical": "0.13.0"
  },
  "scripts": {
    "test": "standard && jest",
    "fix": "standard --fix",
    "refresh": "rm -rf node_modules/ && rm package-lock.json && npm i",
    "homey-build": "browserify lib/variable-management.js --standalone variableMgmt -o settings/variable-management.js && npm run test && homey app build",
    "homey-run": "npm run homey-build && homey app run",
    "homey-install": "npm run homey-build && homey app install",
    "homey-publish": "npm run homey-build && npm run fix && homey app publish",
    "update-dev": "git checkout dev && git pull",
    "update-master": "git checkout master && git pull",
    "even-up-dev": "git checkout dev && git merge master && git push && git checkout master",
    "even-up-master": "git checkout master && git merge dev && git push && git checkout dev"
  },
  "standard": {
    "ignore": [
      "settings/*.js"
    ],
    "env": [
      "jest"
    ]
  },
  "jest": {
    "testMatch": [
      "**/tests/**/*test.[jt]s?(x)",
      "**/?(*.)+.(spec|test).[jt]s?(x)"
    ]
  }
}
