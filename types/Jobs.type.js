/**
 * @typedef {import('croner').Cron} CronJob
 */

/**
 * @typedef {object} Jobs
 * @property {CronJob} trigger - The job to trigger events
 * @property {CronJob} [update] - The job to update calendars
 */
